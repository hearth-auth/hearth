meta {
  name: Introspect Token
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/realms/{{realmName}}/introspect
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "{{accessToken}}",
    "token_type_hint": "access_token",
    "client_id": "{{clientId}}"
  }
}
