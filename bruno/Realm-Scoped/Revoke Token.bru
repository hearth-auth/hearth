meta {
  name: Revoke Token
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/realms/{{realmName}}/revoke
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "{{accessToken}}",
    "token_type_hint": "access_token",
    "client_id": "{{clientId}}"
  }
}
