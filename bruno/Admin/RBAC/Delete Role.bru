meta {
  name: Delete Role
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/roles/{{roleId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
