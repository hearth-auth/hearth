meta {
  name: List Groups
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/admin/groups
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}

query {
  page: 1
  per_page: 20
}
