meta {
  name: Remove Group Member
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/admin/groups/{{groupId}}/members/{{memberId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
