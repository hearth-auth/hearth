meta {
  name: Delete Group
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/admin/groups/{{groupId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
