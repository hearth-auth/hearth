meta {
  name: Cluster Status
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/cluster/status
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
