meta {
  name: Get Effective Permissions
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/admin/users/{{userId}}/effective-permissions
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
