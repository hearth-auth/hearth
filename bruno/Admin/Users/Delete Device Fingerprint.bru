meta {
  name: Delete Device Fingerprint
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/admin/users/{{userId}}/device-fingerprints
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
