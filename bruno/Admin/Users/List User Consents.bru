meta {
  name: List User Consents
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/admin/users/{{userId}}/consents
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
