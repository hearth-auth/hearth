meta {
  name: Create Backup
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/backup
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
