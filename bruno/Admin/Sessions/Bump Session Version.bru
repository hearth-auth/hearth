meta {
  name: Bump Session Version
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/sessions/{{sessionId}}/sv-bump
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
