meta {
  name: Delete Email Template
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/admin/realms/{{realmId}}/email-templates/{{emailTemplateKind}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
