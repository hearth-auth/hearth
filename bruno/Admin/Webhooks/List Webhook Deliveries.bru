meta {
  name: List Webhook Deliveries
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/admin/webhooks/{{webhookId}}/deliveries
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}

query {
  page: 1
  per_page: 20
}
