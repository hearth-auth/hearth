meta {
  name: List WebAuthn Credentials
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/webauthn/credentials
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
