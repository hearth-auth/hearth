meta {
  name: Delete WebAuthn Credential
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/webauthn/credentials/{{credentialId}}
  body: none
  auth: bearer
}

headers {
  X-Realm-ID: {{realmId}}
}

auth:bearer {
  token: {{accessToken}}
}
