syntax = "proto3";

package hearth.events.v1;

import "google/api/annotations.proto";

option go_package = "github.com/hearthdb/hearth/sdks/go/generated/events/v1;eventsv1";

// Categories of security-critical actions recorded in the audit log.
enum AuditAction {
  AUDIT_ACTION_UNSPECIFIED = 0;
  AUDIT_ACTION_USER_CREATED = 1;
  AUDIT_ACTION_USER_UPDATED = 2;
  AUDIT_ACTION_USER_DELETED = 3;
  AUDIT_ACTION_CREDENTIAL_SET = 4;
  AUDIT_ACTION_CREDENTIAL_CHANGED = 5;
  AUDIT_ACTION_CREDENTIAL_VERIFIED = 6;
  AUDIT_ACTION_SESSION_CREATED = 7;
  AUDIT_ACTION_SESSION_REVOKED = 8;
  AUDIT_ACTION_TOKEN_ISSUED = 9;
  AUDIT_ACTION_TOKEN_REFRESHED = 10;
  AUDIT_ACTION_REALM_CREATED = 11;
  AUDIT_ACTION_REALM_UPDATED = 12;
  AUDIT_ACTION_REALM_DELETED = 13;
  AUDIT_ACTION_CLIENT_REGISTERED = 14;
  AUDIT_ACTION_AUTHORIZATION_CODE_ISSUED = 15;
  AUDIT_ACTION_AUTHORIZATION_CODE_EXCHANGED = 16;
  AUDIT_ACTION_TUPLE_WRITTEN = 17;
  AUDIT_ACTION_TUPLE_DELETED = 18;
  AUDIT_ACTION_CLIENT_UPDATED = 19;
  AUDIT_ACTION_CLIENT_DELETED = 20;
  AUDIT_ACTION_BULK_USERS_CREATED = 21;
  AUDIT_ACTION_BULK_USERS_DISABLED = 22;
  AUDIT_ACTION_ORG_CREATED = 23;
  AUDIT_ACTION_ORG_UPDATED = 24;
  AUDIT_ACTION_ORG_DELETED = 25;
  AUDIT_ACTION_CONSENT_GRANTED = 26;
  AUDIT_ACTION_CONSENT_DENIED = 27;
  AUDIT_ACTION_CONSENT_REVOKED = 28;
  AUDIT_ACTION_FEDERATION_LOGIN_STARTED = 29;
  AUDIT_ACTION_FEDERATION_LOGIN_COMPLETED = 30;
  AUDIT_ACTION_FEDERATION_ACCOUNT_LINKED = 31;
  AUDIT_ACTION_FEDERATION_ACCOUNT_UNLINKED = 32;
  AUDIT_ACTION_FEDERATION_JIT_PROVISIONED = 33;
  AUDIT_ACTION_SAML_LOGIN_INITIATED = 34;
  AUDIT_ACTION_SAML_LOGIN_COMPLETED = 35;
  AUDIT_ACTION_SAML_LOGIN_FAILED = 36;
  AUDIT_ACTION_SAML_IDP_AUTHN_REQUEST_RECEIVED = 37;
  AUDIT_ACTION_SAML_IDP_RESPONSE_ISSUED = 38;
  AUDIT_ACTION_SAML_IDP_INITIATED_SSO = 39;
  AUDIT_ACTION_SAML_SLO_REQUESTED = 40;
  AUDIT_ACTION_SAML_SLO_COMPLETED = 41;
  AUDIT_ACTION_SCIM_USER_CREATED = 42;
  AUDIT_ACTION_SCIM_USER_UPDATED = 43;
  AUDIT_ACTION_SCIM_USER_DELETED = 44;
  AUDIT_ACTION_SCIM_GROUP_CREATED = 45;
  AUDIT_ACTION_SCIM_GROUP_UPDATED = 46;
  AUDIT_ACTION_SCIM_GROUP_DELETED = 47;
  AUDIT_ACTION_ROLE_ASSIGNED = 48;
  AUDIT_ACTION_ROLE_REVOKED = 49;
  AUDIT_ACTION_CLEANUP = 50;
  // RBAC group management
  AUDIT_ACTION_GROUP_CREATED = 51;
  AUDIT_ACTION_GROUP_UPDATED = 52;
  AUDIT_ACTION_GROUP_DELETED = 53;
  AUDIT_ACTION_GROUP_MEMBER_ADDED = 54;
  AUDIT_ACTION_GROUP_MEMBER_REMOVED = 55;
  AUDIT_ACTION_GROUP_MEMBER_ROLE_CHANGED = 56;
  // Permission management
  AUDIT_ACTION_ORPHANED_REFERENCE_SKIPPED = 57;
  AUDIT_ACTION_USER_PERMISSION_GRANTED = 58;
  AUDIT_ACTION_USER_PERMISSION_REVOKED = 59;
  AUDIT_ACTION_CLIENT_CONSENT_GRANTED = 60;
  AUDIT_ACTION_CLIENT_CONSENT_REVOKED = 61;
  AUDIT_ACTION_CONSENT_REQUIRED_ON_REFRESH = 62;
  // Login events
  AUDIT_ACTION_LOGIN_FAILED = 63;
  AUDIT_ACTION_LOGIN_LOCKED = 64;
  AUDIT_ACTION_IP_LOGIN_LIMIT_EXCEEDED = 65;
  // Backup and export
  AUDIT_ACTION_BACKUP_CREATED = 66;
  AUDIT_ACTION_BACKUP_RESTORED = 67;
  AUDIT_ACTION_REALM_EXPORT_WATERMARKED = 68;
  // Required actions
  AUDIT_ACTION_REQUIRED_ACTION_ASSIGNED = 69;
  AUDIT_ACTION_REQUIRED_ACTION_REMOVED = 70;
  AUDIT_ACTION_REQUIRED_ACTION_COMPLETED = 71;
  AUDIT_ACTION_REQUIRED_ACTION_AUTO_CLEARED = 72;
  // Password security
  AUDIT_ACTION_PASSWORD_COMPROMISED_REJECTED = 73;
  AUDIT_ACTION_BREACH_CHECK_UNAVAILABLE = 74;
  // Adaptive MFA / step-up
  AUDIT_ACTION_STEP_UP_MFA_TRIGGERED = 75;
  AUDIT_ACTION_STEP_UP_MFA_COMPLETED = 76;
  // SMS OTP enrollment
  AUDIT_ACTION_SMS_OTP_ENROLLMENT_STARTED = 77;
  AUDIT_ACTION_SMS_OTP_ENROLLMENT_VERIFIED = 78;
  AUDIT_ACTION_SMS_OTP_ENROLLMENT_FAILED = 79;
  // SMS MFA challenges
  AUDIT_ACTION_SMS_MFA_CHALLENGE_SUCCEEDED = 80;
  AUDIT_ACTION_SMS_MFA_CHALLENGE_FAILED = 81;
  AUDIT_ACTION_SMS_MFA_LOCKED = 82;
  // Device fingerprints
  AUDIT_ACTION_DEVICE_FINGERPRINTS_ERASED = 83;
  // Session management
  AUDIT_ACTION_SESSION_LIMIT_ENFORCED = 84;
  AUDIT_ACTION_SESSIONS_REVOKED = 85;
  AUDIT_ACTION_SESSION_EVICTED = 86;
  // Abuse detection
  AUDIT_ACTION_ABUSE_DETECTED = 87;
  // Email change
  AUDIT_ACTION_EMAIL_CHANGE_INITIATED = 88;
  AUDIT_ACTION_EMAIL_CHANGE_CONFIRMED = 89;
  // OIDC silent auth
  AUDIT_ACTION_OIDC_SILENT_AUTH_PROBED = 90;
  // Agent lifecycle
  AUDIT_ACTION_AGENT_CREATED = 91;
  AUDIT_ACTION_AGENT_UPDATED = 92;
  AUDIT_ACTION_AGENT_SUSPENDED = 93;
  AUDIT_ACTION_AGENT_REACTIVATED = 94;
  AUDIT_ACTION_AGENT_REVOKED = 95;
  AUDIT_ACTION_AGENT_DELETED = 96;
  AUDIT_ACTION_AGENT_CREDENTIAL_CREATED = 97;
  AUDIT_ACTION_AGENT_CREDENTIAL_REVOKED = 98;
  // Agent delegation and MCP (M2)
  AUDIT_ACTION_AGENT_DELEGATION = 99;
  AUDIT_ACTION_AGENT_TOOL_INVOCATION = 100;
  AUDIT_ACTION_APPROVAL_REQUESTED = 101;
  AUDIT_ACTION_APPROVAL_GRANTED = 102;
  AUDIT_ACTION_APPROVAL_DENIED = 103;
  AUDIT_ACTION_AGENT_TOKEN_REVOKED = 104;
  AUDIT_ACTION_CROSS_REALM_TRUST_CREATED = 105;
  AUDIT_ACTION_CROSS_REALM_TRUST_REVOKED = 106;
  AUDIT_ACTION_PROTECTED_RESOURCE_REGISTERED = 107;
  AUDIT_ACTION_PROTECTED_RESOURCE_UPDATED = 108;
  AUDIT_ACTION_PROTECTED_RESOURCE_DELETED = 109;
  // Phase D — advanced agent surface
  AUDIT_ACTION_AAT_ISSUED = 110;
  AUDIT_ACTION_AAT_REVOKED = 111;
  AUDIT_ACTION_TRANSACTION_TOKEN_ISSUED = 112;
  AUDIT_ACTION_CROSS_REALM_TOKEN_ISSUED = 113;
  AUDIT_ACTION_SPIFFE_ID_MAPPED = 114;
  AUDIT_ACTION_SPIFFE_AUTH_SUCCESS = 115;
}

// A recorded audit event in the append-only log.
message AuditEvent {
  string id = 1;
  string realm_id = 2;
  string actor = 3;
  AuditAction action = 4;
  string resource_type = 5;
  string resource_id = 6;
  int64 timestamp = 7;
  // Optional additional context (JSON-encoded).
  optional string metadata = 8;
  string integrity_hash = 9;
}

// Request to append a new audit event.
message CreateAuditEvent {
  string realm_id = 1;
  string actor = 2;
  AuditAction action = 3;
  string resource_type = 4;
  string resource_id = 5;
  // Optional additional context (JSON-encoded).
  optional string metadata = 6;
}

// Query parameters for filtering audit events.
// All fields except realm_id become URL query parameters on the HTTP binding.
message AuditQuery {
  string realm_id = 1;
  optional int64 start_time = 2;
  optional int64 end_time = 3;
  optional string actor = 4;
  optional AuditAction action = 5;
  optional uint32 limit = 6;
}

// A page of audit events.
message AuditEventPage {
  repeated AuditEvent events = 1;
}

// Request to verify the integrity hash chain of the realm's audit log.
// Realm context comes from the `x-realm-id` metadata (admin interceptor).
message VerifyIntegrityRequest {}

// Result of an integrity check. `ok = true` means the hash chain verified
// end-to-end; `ok = false` indicates a broken link at `broken_at_event_id`.
message VerifyIntegrityResponse {
  bool ok = 1;
  optional string broken_at_event_id = 2;
  uint64 event_count = 3;
}

service AuditService {
  // Query audit events with optional filters. Maps to GET /admin/audit.
  rpc ListEvents(AuditQuery) returns (AuditEventPage) {
    option (google.api.http) = {get: "/admin/audit"};
  }
  // Verify the integrity hash chain — gRPC-only (no REST route yet).
  rpc VerifyIntegrity(VerifyIntegrityRequest) returns (VerifyIntegrityResponse);
}
