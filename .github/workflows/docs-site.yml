name: Docs Site

on:
  push:
    branches: [main]
    paths:
      - 'docs-site/**'
      - 'docs/guides/**'
  # Allow manual trigger from the Actions tab.
  workflow_dispatch:

# Required for GitHub Pages OIDC deployment.
permissions:
  contents: read
  pages: write
  id-token: write

# Only one deployment runs at a time; cancel any in-flight build/deploy pair.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: docs-site/package-lock.json

      - name: Configure Pages
        uses: actions/configure-pages@1f0c5cde4bc74cd7e1254d0cb4de8d49e9068c7d # v4

      - name: Install dependencies
        working-directory: docs-site
        run: npm ci

      - name: Build
        working-directory: docs-site
        run: npm run build
        env:
          # Pass the Pages base URL so Docusaurus generates correct asset paths
          # when deployed to a subpath (e.g. org.github.io/hearth/).
          # configure-pages sets ACTIONS_STEP_SUMMARY; we read it via the action output.
          NODE_ENV: production

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: docs-site/build

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
